"use client";

import { type FormEvent, useMemo, useState } from "react";
import type { FeatureLabItem } from "@/lib/types";
import type { SourceOverview } from "@/lib/dashboard-data";

const filterOptions = ["Alle", "Recherche", "Bewerten", "Nachbauen", "Erledigt"] as const;
const classFor = (value: string) => value.toLowerCase().replaceAll(" ", "-").replaceAll("ä", "ae").replaceAll("ö", "oe").replaceAll("ü", "ue");

export default function FeatureLab({ items, sources }: { items: FeatureLabItem[]; sources: SourceOverview[] }) {
  const [showForm, setShowForm] = useState(false); const [saving, setSaving] = useState(false); const [message, setMessage] = useState(""); const [filter, setFilter] = useState<(typeof filterOptions)[number]>("Alle");
  const visible = useMemo(() => filter === "Alle" ? items : items.filter((item) => item.status === filter), [items, filter]);
  async function save(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setSaving(true); setMessage(""); const form = new FormData(event.currentTarget); const response = await fetch("/api/feature-lab", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ publisherSourceId: form.get("publisherSourceId"), productName: form.get("productName"), featureDescription: form.get("featureDescription"), referenceUrl: form.get("referenceUrl"), screenshotUrl: form.get("screenshotUrl"), copyability: form.get("copyability"), implementationEffort: form.get("implementationEffort"), visibility: form.get("visibility"), productValue: form.get("productValue"), buildPriority: form.get("buildPriority"), status: form.get("status"), rationale: form.get("rationale"), technicalNotes: form.get("technicalNotes") }) }); const body = await response.json(); setSaving(false); if (!response.ok) { setMessage(body.error ?? "Feature konnte nicht gespeichert werden."); return; } window.location.reload(); }
  async function copyBrief(item: FeatureLabItem) { const brief = `Baue dieses Publisher-Feature nach.\n\nProdukt: ${item.productName}\nPublisher: ${item.publisher}\nFeature: ${item.featureDescription}\nReferenz: ${item.referenceUrl}\nKopierbarkeit: ${item.copyability}\nAufwand: ${item.implementationEffort}\nProduktnutzen: ${item.productValue}\nTechnische Hinweise: ${item.technicalNotes || "Noch zu recherchieren."}\n\nErstelle zuerst eine Umsetzungsanalyse mit UI-Ablauf, Datenmodell, Komponentenstruktur, Risiken und einem schrittweisen Implementierungsplan.`; await navigator.clipboard.writeText(brief); setMessage("Codex-Briefing in die Zwischenablage kopiert."); }
  return <section className="feature-lab"><div className="lab-heading"><div><p className="eyebrow">FEATURE-LAB / NACHBAU-BACKLOG</p><h2>Von der Beobachtung zur <em>Umsetzung</em></h2><p>Belegte Publisher-Features bewerten, priorisieren und als Codex-Aufgabe vorbereiten.</p></div><button className="outline" onClick={() => setShowForm((current) => !current)}>{showForm ? "Abbrechen" : "+ Feature erfassen"}</button></div><div className="lab-toolbar">{filterOptions.map((option) => <button key={option} className={filter === option ? "selected" : ""} onClick={() => setFilter(option)}>{option}</button>)}</div>{showForm && <form className="lab-form" onSubmit={save}><label>Verlag<select name="publisherSourceId" required defaultValue=""><option value="" disabled>Publisher auswählen</option>{sources.map((source) => <option value={source.id} key={source.id}>{source.name}</option>)}</select></label><label>Produktname<input name="productName" required placeholder="z. B. Daily Audio Briefing" /></label><label>Feature-Beschreibung<textarea name="featureDescription" required placeholder="Was sehen und tun Nutzer:innen konkret?" /></label><label>Referenzlink<input name="referenceUrl" type="url" required placeholder="https://…" /></label><label>Screenshot-URL <small>optional</small><input name="screenshotUrl" type="url" placeholder="https://…/screenshot.png" /></label><label>Kopierbarkeit<select name="copyability" defaultValue="medium"><option value="high">Hoch</option><option value="medium">Mittel</option><option value="low">Niedrig</option></select></label><label>Umsetzungsaufwand<select name="implementationEffort" defaultValue="medium"><option value="low">Niedrig</option><option value="medium">Mittel</option><option value="high">Hoch</option></select></label><label>Sichtbarkeit<select name="visibility" defaultValue="clear"><option value="clear">Klar sichtbar</option><option value="partial">Teilweise sichtbar</option><option value="unclear">Unklar</option></select></label><label>Produktnutzen<select name="productValue" defaultValue="medium"><option value="high">Hoch</option><option value="medium">Mittel</option><option value="low">Niedrig</option></select></label><label>Nachbaupriorität<select name="buildPriority" defaultValue="watch"><option value="now">Jetzt</option><option value="watch">Beobachten</option><option value="later">Später</option></select></label><label>Backlog-Status<select name="status" defaultValue="research"><option value="research">Recherche</option><option value="evaluate">Bewerten</option><option value="build">Nachbauen</option><option value="done">Erledigt</option></select></label><label>Begründung<textarea name="rationale" placeholder="Warum ist das Feature interessant?" /></label><label>Technische Hinweise<textarea name="technicalNotes" placeholder="UI-Ablauf, technische Annahmen, offene Fragen" /></label><button className="primary" disabled={saving}>{saving ? "Speichert…" : "Feature ins Lab übernehmen"}</button></form>}{message && <p className="form-message">{message}</p>}<div className="lab-grid">{visible.length ? visible.map((item) => <article key={item.id}><header><span className={`lab-status ${classFor(item.status)}`}>{item.status}</span><span className={`lab-priority ${classFor(item.buildPriority)}`}>{item.buildPriority}</span></header>{item.screenshotUrl && <img src={item.screenshotUrl} alt={`Screenshot: ${item.productName}`} />}<p className="lab-publisher">{item.publisher}</p><h3>{item.productName}</h3><p>{item.featureDescription}</p><div className="lab-facts"><span>Kopierbarkeit: <b>{item.copyability}</b></span><span>Aufwand: <b>{item.implementationEffort}</b></span><span>Nutzen: <b>{item.productValue}</b></span><span>{item.visibility}</span></div>{item.rationale && <small>{item.rationale}</small>}<footer><a href={item.referenceUrl} target="_blank" rel="noreferrer">Referenz öffnen ↗</a><button className="outline" onClick={() => copyBrief(item)}>Codex-Briefing kopieren</button></footer></article>) : <div className="empty-state"><strong>Das Feature-Lab ist noch leer.</strong><span>Erfasse eine belegte Publisher-Beobachtung als priorisierte Nachbauaufgabe.</span></div>}</div></section>;
}
