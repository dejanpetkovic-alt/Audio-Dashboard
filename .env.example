# Shared dashboard access. Store both values only in your deployment platform.
AUTH_SECRET=replace-with-a-long-random-secret
DASHBOARD_PASSWORD=replace-with-a-long-unique-shared-password

# Persistence and automated ingestion (to be wired in the deployment environment)
DATABASE_URL=postgresql://user:password@localhost:5432/media_pulse
OPENAI_API_KEY=
